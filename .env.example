# hairhealth.ai integration (optional).
# While unset, the diagnosis flow uses the local questionnaire-based analysis model.
# When set, AnalyzingStep POSTs the uploaded photos + questionnaire to this endpoint
# via src/domain/analysis/hairhealthAdapter.ts (confirm that adapter's request/response
# contract against hairhealth.ai's real API docs before going live).
VITE_HAIRHEALTH_API_URL=
VITE_HAIRHEALTH_API_KEY=
