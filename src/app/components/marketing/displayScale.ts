// Shared font-size for the oversized editorial display headings: page hero
// titles and the numbered section headings across the marketing site. Kept in
// one place so the scale can be tuned globally.
export const DISPLAY_CLAMP = 'clamp(1.75rem, 8vw, 6rem)';
